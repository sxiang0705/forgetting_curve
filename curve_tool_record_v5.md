# 遺忘曲線提醒工具（`curve.py`）更新紀錄（v5-極簡畫框版）

本次 v5 版推出了全新的**背景個人化**功能，讓你可以打造自己專屬的應用程式風格！

---

## 1) 最新概念：畫框佈景模式 (Wallpaper Mode)
- 由於 Windows 底層元件的限制，如果在操作區塊疊加透明層容易造成滑鼠點擊被攔截。
- **解決方案：** 我們全面採用了最穩定、最大氣的「畫框模式」！
- 現在，你上傳的背景圖片會安安穩穩地鋪在程式的**最底層**。
- 同時，我們將主操作區塊（任務清單與月曆）的邊緣大幅向內縮（留出 60px 的超大邊距），讓你的背景圖片就像「電腦桌面」或是「精緻相框」一樣，從四周優雅地透出來！
- 優點：**絕對保證 100% 點擊順暢**，完全不干擾你的任何管理與操作！

## 2) 新 UI：個人化管理按鈕與彈窗
- **主畫面按鈕**：在左下角的控制區中新增了一個「個人化」按鈕。
- **背景設定彈窗**：點擊後會開啟「個人化背景設定」視窗，內含上方控制列與下方的歷史背景縮圖清單。

## 3) 圖片上傳與預覽
- 點擊「上傳新圖片」即可選擇 `.jpg`, `.jpeg`, `.png`, `.webp` 等格式。
- **檔案限制**：內建 20MB 大小限制防誤傳。
- **儲存機制**：圖片會自動複製並存入專屬的 `backgrounds` 資料夾，寫入 SQLite `backgrounds` 資料表供歷史紀錄使用。
- **縮圖呈現**：上傳的圖片會立刻成生小型預覽圖顯示在列表中。

## 4) 背景套用與刪除
- **套用背景**：只要點擊「設為背景」，這張背景圖便會立刻套用到畫框底層！
- **刪除背景**：如果發現不喜歡的背景，點擊「刪除」會連同實體檔案與紀錄一起清空；若剛好刪除的是正在使用中的背景，系統也會自動順延下一張圖片。

## 5) 啟動時隨機切換
- 在個人化彈窗右上角有一個「啟動程式時隨機切換背景」的核取方塊。
- 只要打勾，程式每次重新啟動時，系統就會從你上傳過的歷史圖庫中隨機挑選一張當成今天的背景相框，為每天的任務帶來全新的驚喜感！
