# 遺忘曲線提醒工具（`curve.py`）更新紀錄（v5-客製化透視表格版）

本次 v5 版經歷了一場重大的底層技術大躍進！為了在「任務清單」區塊完美呈現你個人的專屬圖片，我們重新打造了整個介面表單元件。

---

## 1) 突破極限：自製高階透明表格組件
- 由於 Windows 與 Tkinter 原生的任務清單 (`Treeview`) 長年以來拒絕支援任何種類的圖片背景，導致我們若要把照片放上去，就會產生嚴重的視覺遮擋或按鈕失效的系統層 Bug。
- **解決方案：** 我們**完全捨棄**了原生的表格引擎，利用高自由度的畫布系統 (`Canvas`) 從零開始親手寫出了專屬於本軟體的 **「透明任務表格 (TransparentTaskTree)」**！
- 現在，你上傳的個人化背景會完美、精準地嵌入在左側的任務清單正後方。
- **動態半透明遮罩**：每一列的任務資料，我們利用 Python 影像引擎 (PIL) 即時產生高質感的「半透明玻璃底色 (Alpha-blended)」，讓黑白相間的任務清單清清楚楚地漂浮在你的自選圖片上，既有質感又超級順暢！

## 2) 保留流暢操作體驗
- 儘管是我們手刻的表格，但原有的精華操作全數完美保留：
  - **支援滑鼠點擊**：點擊任一任務，表單依舊會自動在右方帶出資料，並且會反白標記。
  - **支援滑鼠滾輪**：長長的任務清單一樣支援滑鼠無縫上下滾動。
  - **滑鼠雙擊**：原有的雙擊（開啟任務明細等）操作依舊靈敏。

## 3) 新 UI：個人化管理按鈕與彈窗
- **主畫面按鈕**：在左下角的控制區中新增一個「個人化」按鈕。
- **背景設定彈窗**：點擊後會開啟背景設定視窗，內含管理歷史縮圖的區塊。

## 4) 圖片上傳與預覽
- 點擊「上傳新圖片」即可選擇 `.jpg`, `.jpeg`, `.png`, `.webp` 等格式。
- **檔案防誤傳**：內建 20MB 大小限制。
- **安全儲存**：圖片會自動複製並存入專屬的 `backgrounds` 資料夾，同步寫入 `reminder.db` 資料表。

## 5) 極度流暢的「隨觸即發」體驗
- **套用背景**：在歷史清單中，你可以「隨處點擊」（標題、照片、或區塊的任何地方），左側的主題背景便會**立刻重新計算尺寸並無縫渲染**套用到你專屬的透明任務清單上！
- **刪除背景**：發現不喜歡的背景可隨時使用刪除功能，檔案與紀錄會一併消失。

## 6) 啟動時隨機切換
- 彈窗右上角有一個「啟動程式時隨機切換背景」的核取方塊。
- 打勾後，每次重新啟動程式，系統就會從圖庫中隨機挑出新風貌，為每天的任務帶來全新的驚喜感！
